"""Параметры дизельного main bearing по Sun et al. (2019).

Источник: Sun et al., "Effects of Textures on the Dynamic Characteristics
of Main Bearings in Internal Combustion Engines", Chinese Journal of
Mechanical Engineering 32:23 (2019).
"""

# ─── Геометрия ─────────────────────────────────────────────────────
R = 0.040              # радиус journal, м (диаметр 80 мм)
L_bearing = 0.027      # ширина подшипника, м
c = 0.000146           # радиальный зазор, м (146 мкм)

# ─── Масло ─────────────────────────────────────────────────────────
eta = 0.014347         # динамическая вязкость, Па·с

# ─── Режим ─────────────────────────────────────────────────────────
n_rpm = 2200           # обороты (начальный кейс)
load_pct = 100         # процент нагрузки (для масштабирования surrogate)

# ─── Двигатель ─────────────────────────────────────────────────────
n_cylinders = 4
n_strokes = 4          # 4-тактный → 720° CA = один цикл

# Surrogate ICE parameters (для build_load_from_indicator.py)
bore_m = 0.080         # диаметр цилиндра, м (совпадает с диаметром journal)
stroke_m = 0.090       # ход поршня, м
con_rod_m = 0.150      # длина шатуна, м
m_piston_kg = 0.5      # масса поршневой группы, кг
p_max_MPa = 8.0        # пиковое давление газа (100% load)
p_motoring_MPa = 2.0   # давление на такте сжатия

# ─── Сетка ─────────────────────────────────────────────────────────
N1 = 200               # физические ячейки по φ
N2 = 20                # физические ячейки по Z

# ─── Начальные условия ─────────────────────────────────────────────
X0 = 0.0
Y0 = 0.6

# ─── Параметры расчёта ─────────────────────────────────────────────
dt = 1e-3              # безразмерный шаг
max_cycles = 20        # максимум циклов
warmup_cycles = 5      # минимум циклов до оценки convergence

# ─── Solver ────────────────────────────────────────────────────────
omega_p = 1.0
omega_theta = 1.0
tol_inner = 1e-6
max_inner = 5000
scheme = "rb"

# ─── Критические пороги (postprocessing) ───────────────────────────
h_crit_um = [5, 10]    # мкм
p_crit_MPa = [50, 100] # МПа
