"""Параметры микротекстуры для подшипника насоса (Payvar-Salant JFO).

Три конфигурации лунок в дивергентной зоне.
Профиль: smoothcap (1 − r²)² — C¹-непрерывный (НЕ sqrt).

Угловая конвенция (из bearing_model.make_H):
    H = 1 + ε·cos(φ)  →  min при φ=π, max при φ=0.
    Дивергентная зона: φ ∈ (π, 2π) = 180°–360°.

Источники: Brizmer & Kligerman (2012), Gropper et al. (2016).
"""

# ─── Конфиг A: baseline (крупные мелкие) ──────────────────────────
# 450 лунок, Sp ≈ 4.6 %, hp/h_min ≈ 0.15 при ε=0.6
# Сетка подобрана под ≥10 узлов/лунку:
#   φ: 2*0.4e-3/0.035 / (2π/2800) ≈ 10.2,  Z: 2*0.5e-3/0.028 / (2/570) ≈ 10.2
CONFIG_A = dict(
    label="A",
    a_dim=0.5e-3,          # полуось Z (м)
    b_dim=0.4e-3,          # полуось φ (м), a/b = 1.25
    h_p=3e-6,              # глубина 3 мкм
    phi_start_deg=180,
    phi_end_deg=360,
    N_phi_tex=30,
    N_Z_tex=15,
    N_phi=2800,
    N_Z=570,
)

# ─── Конфиг B: основной (средние плотные) ─────────────────────────
# 1800 лунок, Sp ≈ 6.9 %, hp/h_min ≈ 0.25 при ε=0.6
# Сетка: φ: 2*0.25e-3/0.035 / (2π/4500) ≈ 10.2, Z: 2*0.3e-3/0.028 / (2/950) ≈ 10.2
CONFIG_B = dict(
    label="B",
    a_dim=0.3e-3,
    b_dim=0.25e-3,         # a/b = 1.2
    h_p=5e-6,              # глубина 5 мкм
    phi_start_deg=180,
    phi_end_deg=360,
    N_phi_tex=60,
    N_Z_tex=30,
    N_phi=4500,
    N_Z=950,
)

# ─── Конфиг C: максимальный эффект (мелкие частые) ────────────────
# 7200 лунок, Sp ≈ 8.3 %, круглые
# Сетка: φ: 2*0.15e-3/0.035 / (2π/7500) ≈ 10.2, Z: 2*0.15e-3/0.028 / (2/1900) ≈ 10.2
CONFIG_C = dict(
    label="C",
    a_dim=0.15e-3,
    b_dim=0.15e-3,         # круглые, a/b = 1.0
    h_p=5e-6,              # глубина 5 мкм
    phi_start_deg=180,
    phi_end_deg=360,
    N_phi_tex=120,
    N_Z_tex=60,
    N_phi=7500,
    N_Z=1900,
)

ALL_CONFIGS = [CONFIG_A, CONFIG_B, CONFIG_C]
