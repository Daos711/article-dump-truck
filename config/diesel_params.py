"""Параметры подшипника ДВС карьерного самосвала (типа БелАЗ).
Геометрия приближена к тяжёлому дизельному двигателю.
"""
R = 0.100           # Радиус подшипника (м) — коренной подшипник
c = 0.00012         # Радиальный зазор (м) = 120 мкм
L = 0.080           # Длина подшипника (м) = 80 мм
n = 1900            # Частота вращения (об/мин) — номинальная для дизеля
m_shaft = 200.0     # Масса, приходящаяся на один подшипник (кг)

# Текстура
# Для ДВС (105°C, низкая вязкость, тонкая плёнка h0 ≈ 5-15 мкм):
# оптимальная глубина h_d ≈ 10-20 мкм (K = h_d/h0 ≈ 1.5-3)
h_p = 0.000015      # Глубина углубления (м) = 15 мкм
a_dim = 0.003       # Полуось вдоль Z (м)
b_dim = 0.0028      # Полуось вдоль φ (м)
N_phi_tex = 10      # Углублений по φ
N_Z_tex = 8         # Углублений по Z
phi_start_deg = 90
phi_end_deg = 270

# Нагрузка F(φ) — Вибе-функция + КШМ-разложение
lambda_crank = 0.27     # R_кривошипа / L_шатуна (БелАЗ)
F_max = 850000.0    # Пиковая нагрузка (Н) = 850 кН (production)
F_max_debug = 200000.0  # Для отладки (Н) = 200 кН
sigma_deg = 25.0    # Ширина пика нагрузки (°)
F_base = 20000.0    # Минимальная нагрузка (Н)

# Нестационарные параметры
N_grid_transient = 200   # Сетка 200×200
d_phi_crank_deg = 0.5    # Шаг по углу ПКВ (°)
n_cycles = 2             # Число циклов
eps_x0 = 0.0             # Начальный εx
eps_y0 = -0.6            # Начальный εy (вал ниже центра, εy < 0)
eps_max = 0.95           # Порог контакта
